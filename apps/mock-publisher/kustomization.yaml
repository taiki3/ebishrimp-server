apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: iot
resources:
  # MQTT パスワードは apps/rumqttd/secret-auth.enc.yaml に一本化した。
  # 旧 secret-env.enc.yaml は削除済み。
  - deployment.yaml
