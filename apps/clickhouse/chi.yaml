apiVersion: clickhouse.altinity.com/v1
kind: ClickHouseInstallation
metadata:
  name: iot
spec:
  defaults:
    templates:
      podTemplate: clickhouse
      dataVolumeClaimTemplate: data
      serviceTemplate: internal
  configuration:
    files:
      # ClickHouse 内蔵のシステムログはデフォルト全部有効。室内IoT規模では
      # 実データ (6 MiB) に対し trace_log が 15.8 GiB まで膨れ、metric_log
      # (超横長テーブル) のマージが 4Gi limit を超えて失敗→無限リトライし、
      # CPU 2コアを常時食った上に ingester の insert まで道連れにした。
      # メトリクスは Prometheus 側で見ているのでプロファイリング系は落とす。
      config.d/system-logs.xml: |
        <clickhouse>
          <trace_log remove="1"/>
          <metric_log remove="1"/>
          <asynchronous_metric_log remove="1"/>
          <latency_log remove="1"/>
          <processors_profile_log remove="1"/>
          <!-- 障害調査に要るものは残すが TTL で頭を抑える -->
          <query_log>
            <ttl>event_date + INTERVAL 3 DAY DELETE</ttl>
          </query_log>
          <query_views_log>
            <ttl>event_date + INTERVAL 3 DAY DELETE</ttl>
          </query_views_log>
          <part_log>
            <ttl>event_date + INTERVAL 3 DAY DELETE</ttl>
          </part_log>
          <error_log>
            <ttl>event_date + INTERVAL 3 DAY DELETE</ttl>
          </error_log>
          <text_log>
            <!-- 既定の trace だと Debug が 5分で3万行入る -->
            <level>information</level>
            <ttl>event_date + INTERVAL 3 DAY DELETE</ttl>
          </text_log>
        </clickhouse>
    clusters:
      - name: main
        layout:
          shardsCount: 1
          replicasCount: 1
    users:
      admin/k8s_secret_password: iot/clickhouse-credentials/admin-password
      admin/networks/ip: "::/0"
      admin/access_management: "1"
      ingester/k8s_secret_password: iot/clickhouse-credentials/ingester-password
      ingester/networks/ip: "::/0"
      dashboard/k8s_secret_password: iot/clickhouse-credentials/dashboard-password
      dashboard/networks/ip: "::/0"
      dashboard/profile: readonly
    profiles:
      readonly/readonly: "2"
  templates:
    serviceTemplates:
      - name: internal
        spec:
          type: ClusterIP
          ports:
            - name: http
              port: 8123
            - name: tcp
              port: 9000
    podTemplates:
      - name: clickhouse
        spec:
          containers:
            - name: clickhouse
              image: clickhouse/clickhouse-server:25.3
              resources:
                requests:
                  cpu: 500m
                  memory: 1Gi
                limits:
                  memory: 4Gi
    volumeClaimTemplates:
      - name: data
        spec:
          accessModes: [ReadWriteOnce]
          storageClassName: local-path
          resources:
            requests:
              storage: 50Gi
